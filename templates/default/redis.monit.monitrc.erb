
check process redis_<%= @name %> with pidfile "<%= @pidfile %>"
    start program = "/etc/init.d/redis_<%= @name %> start"
    stop program = "/etc/init.d/redis_<%= @name %> stop"
    if failed host 127.0.0.1 port <%= @port %> then restart
    if 5 restarts within 5 cycles then timeout